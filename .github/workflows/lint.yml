name: Auto linter

on: 
  push: 
    paths: "*.py"

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5

# this step install Lintin tool
      - name: Install Linter
        run: pip install ruff

      - name: Check the file for linter
        run: ruff check .
